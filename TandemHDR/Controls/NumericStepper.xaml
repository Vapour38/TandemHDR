<UserControl x:Class="TandemHdr.Controls.NumericStepper"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             x:ClassModifier="internal"
             d:DesignWidth="96" d:DesignHeight="28">
    <UserControl.Resources>
        <Style x:Key="StepperButtonStyle" TargetType="RepeatButton">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Height" Value="13" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                            <Viewbox Width="8" Height="4" Stretch="Uniform">
                                <ContentPresenter />
                            </Viewbox>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="{DynamicResource RowHoverBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Border BorderThickness="1"
            CornerRadius="5"
            Background="{DynamicResource ControlBackgroundBrush}"
            BorderBrush="{DynamicResource ControlBorderBrush}"
            x:Name="Frame">
        <Grid Height="28">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="20" />
            </Grid.ColumnDefinitions>

            <TextBox x:Name="ValueBox"
                     Grid.Column="0"
                     BorderThickness="0"
                     Background="Transparent"
                     Foreground="{DynamicResource ForegroundPrimaryBrush}"
                     VerticalContentAlignment="Center"
                     HorizontalContentAlignment="Right"
                     Padding="0,0,4,0"
                     FontSize="13"
                     PreviewTextInput="OnPreviewTextInput"
                     LostFocus="OnValueBoxLostFocus"
                     KeyDown="OnValueBoxKeyDown" />

            <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center">
                <RepeatButton x:Name="UpButton" Style="{StaticResource StepperButtonStyle}" Click="OnUpClick">
                    <Path Data="M 0,4 L 4,0 L 8,4" Stroke="{DynamicResource ForegroundSecondaryBrush}" StrokeThickness="1.3" StrokeStartLineCap="Round" StrokeEndLineCap="Round" />
                </RepeatButton>
                <RepeatButton x:Name="DownButton" Style="{StaticResource StepperButtonStyle}" Click="OnDownClick">
                    <Path Data="M 0,0 L 4,4 L 8,0" Stroke="{DynamicResource ForegroundSecondaryBrush}" StrokeThickness="1.3" StrokeStartLineCap="Round" StrokeEndLineCap="Round" />
                </RepeatButton>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
